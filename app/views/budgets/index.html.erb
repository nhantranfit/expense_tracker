<div class="max-w-5xl mx-auto p-6 bg-gray-900 min-h-screen text-white">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-3xl font-bold text-orange-400">💰 Ngân sách</h2>
    <%= link_to "➕ Thêm ngân sách", new_budget_path, class: "bg-orange-500 hover:bg-orange-600 text-white px-4 py-2 rounded shadow" %>
  </div>

  <div class="overflow-x-auto rounded">
    <table class="min-w-full border border-gray-700 bg-gray-800 rounded shadow-md">
      <thead>
        <tr class="bg-gray-700 text-orange-300 uppercase text-sm">
          <th class="px-4 py-2 text-left border-b border-gray-600">Danh mục</th>
          <th class="px-4 py-2 text-left border-b border-gray-600">Chu kỳ</th>
          <th class="px-4 py-2 text-left border-b border-gray-600">Hạn mức</th>
          <th class="px-4 py-2 text-left border-b border-gray-600">Hành động</th>
        </tr>
      </thead>
      <tbody>
        <% @budgets.each do |budget| %>
          <tr class="hover:bg-gray-700 transition-colors duration-200">
            <td class="px-4 py-2 border-b border-gray-700">
              <% if budget.category %>
                <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium" style="background-color: <%= budget.category.color %>20; color: <%= budget.category.color %>">
                  <%= budget.category.name %>
                </span>
              <% else %>
                <span class="text-gray-500 text-xs">Không có</span>
              <% end %>
            </td>
            <td class="px-4 py-2 border-b border-gray-700">
              <% case budget.period
                when 'weekly' %>Tuần<% when 'monthly' %>Tháng<% when 'yearly' %>Năm<% else %><%= budget.period %><% end %>
            </td>
            <td class="px-4 py-2 border-b border-gray-700 text-orange-300 font-medium"><%= number_with_delimiter(budget.amount, delimiter: ",") %> ₫</td>
            <td class="px-4 py-2 border-b border-gray-700 text-sm">
              <div class="flex items-center space-x-3">
                <%= link_to "✏️ Sửa", edit_budget_path(budget), class: "text-orange-400 hover:underline" %>
                <%= button_to "🗑️ Xoá", budget_path(budget), method: :delete, data: { turbo: false, confirm: "Bạn chắc không?" }, class: "text-red-400 hover:underline", form: { class: "inline" } %>
              </div>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <% if @budgets.empty? %>
    <div class="text-center py-12">
      <p class="text-gray-400 text-lg">Chưa có ngân sách nào. Hãy tạo ngân sách đầu tiên!</p>
    </div>
  <% end %>
</div>
