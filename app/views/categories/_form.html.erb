<%= form_with(model: category, local: true, html: { class: "max-w-xl mx-auto p-6 bg-gray-900 text-white rounded shadow" }) do |form| %>
  <% if category.errors.any? %>
    <div class="mb-4 p-4 border border-red-400 bg-red-900 text-red-200 rounded">
      <h3 class="font-semibold mb-2"><%= pluralize(category.errors.count, "lỗi") %>:</h3>
      <ul class="list-disc list-inside text-sm">
        <% category.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-4">
    <%= form.label :name, "📂 Tên danh mục", class: "block mb-1 text-sm font-medium text-orange-300" %>
    <%= form.text_field :name, class: "w-full px-3 py-2 rounded bg-gray-800 border border-gray-600 text-white focus:outline-none focus:ring-2 focus:ring-orange-500" %>
  </div>

  <div class="mb-4">
    <%= form.label :color, "🎨 Màu sắc", class: "block mb-1 text-sm font-medium text-orange-300" %>
    <%= form.color_field :color, class: "w-full px-3 py-2 rounded bg-gray-800 border border-gray-600 text-white focus:outline-none focus:ring-2 focus:ring-orange-500" %>
  </div>

  <div class="flex items-center justify-between mt-6">
    <%= form.submit "💾 Lưu", class: "bg-orange-500 hover:bg-orange-600 text-white font-semibold px-5 py-2 rounded shadow" %>
    <%= link_to "⬅ Quay lại", categories_path, class: "text-orange-300 hover:underline text-sm" %>
  </div>
<% end %>
